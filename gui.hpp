#include "central.hpp"

enum class UI : bool{
    BUTTON,
    TEXT,
    CHECKBOX,
    SLIDER,
    DROPDOWN,
    TOGGLE,

};

enum class Color {
    RED,
    GREEN,
    BLUE,
    WHITE,
};

enum class Shape2D : bool{
    CIRCLE,
    SQUARE,
    TRIANGLE,
    RECTANGLE,
};

