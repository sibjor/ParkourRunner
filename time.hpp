#include <chrono>
#include <ctime>
#include <iomanip>

enum class Time
{
    NANOSECOND,
    MICROSECOND,
    MILLISECOND,
    SECOND,
    MINUTE,
    HOUR, 
};

enum class Date{

    DAY,
    MONTH,
    YEAR,
};